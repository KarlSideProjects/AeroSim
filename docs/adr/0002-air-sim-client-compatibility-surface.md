# Support a frozen subset of existing AirSim clients

Lab Mode will define a frozen AirSim Compatibility Surface on which existing AirSim clients work unchanged. A bounded compatibility contract reuses the AirSim ecosystem and its examples without committing AeroSim to every historical AirSim feature; operations outside the published surface must report that they are unsupported.
