# Freeze the primary navigation

The main menu will expose exactly seven first-level entries: `Quick Fly`, `Lab Mode`, `Controller`, `Drone`, `Map`, `Settings`, and `Quit`. This keeps the game-like flight path, simulator operations, hardware setup, catalog selections, configuration, and desktop exit directly discoverable without nesting unrelated tasks.
