# Support two simultaneous named multirotors

The AirSim-class minimum will support two simultaneous named multirotors in Lab Mode while Player Mode controls one primary vehicle. Two vehicles are enough to establish per-vehicle control and sensor isolation, multi-vehicle API semantics, and downwash interaction without committing the first release to arbitrary fleet scale.
