# Restrict runtime scene objects to an approved catalog

Lab Mode may spawn and destroy named Scene Objects only from a checked-in catalog that defines visual assets, collision, and segmentation labels. Arbitrary file paths and runtime model import are excluded because they make licensing, validation, deterministic replay, and dataset semantics unverifiable.
