# Freeze the baseline sensor suite

The AirSim-class minimum will provide RGB, depth, segmentation, IMU, GPS, magnetometer, barometer, and LiDAR observations. Optical flow, distance sensors, and object detection remain extensions because the baseline image and LiDAR outputs already cover their first-order use cases without expanding the initial compatibility surface.
