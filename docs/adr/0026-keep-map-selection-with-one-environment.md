# Keep map selection with one environment

The main menu will retain a `Map` entry and a catalog-backed selection screen even though the AirSim-class minimum contains only the Industrial Test Range. The screen shows only real selectable environments and no placeholder maps; keeping the stable navigation surface allows later environments to be added without redesigning Player Mode while preserving the one-map delivery boundary.
