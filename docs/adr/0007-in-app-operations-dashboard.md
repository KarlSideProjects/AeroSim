# Keep the operations dashboard inside Godot

The Operations Dashboard will be a native Godot Control interface within the simulator, compact in Player Mode and expanded in Lab Mode. A separate web or desktop dashboard would duplicate state synchronization, input, packaging, and visual QA without improving the first AirSim-class release.
