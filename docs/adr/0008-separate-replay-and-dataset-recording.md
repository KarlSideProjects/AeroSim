# Treat flight replay and dataset recording as separate capabilities

The AirSim-class minimum will retain deterministic Flight Replay and add synchronized Dataset Recording for both Vehicle Instances and enabled sensors. Replay proves reproducibility; dataset recording serves analysis and AI workflows, so neither capability may be used as evidence that the other is complete.
